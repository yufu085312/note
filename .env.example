# note のログインは save_session.py で1回だけ手動ログインし、
# storage_state.json にセッションを保存して再利用します（.env に認証情報は不要）。

# AIで記事生成する場合（formatter の use_ai=true 時）
# 無料キー: https://aistudio.google.com/apikey で取得（無料枠内なら課金なし）
GEMINI_API_KEY=
